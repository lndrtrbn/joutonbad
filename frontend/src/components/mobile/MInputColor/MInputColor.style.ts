const MInputColorStyle = {
  base: "flex items-start gap-4 cursor-pointer",
  preview: "w-6 h-6 rounded-full overflow-hidden relative",
  input:
    "absolute left-1/2 -translate-x-1/2 top-1/2 -translate-y-1/2 h-12 w-12",
  labels: "flex items-start gap-2 flex-col mt-[2px] select-none",
};

export default MInputColorStyle;
